import XCTest
import Nimble
@testable import JSONMatcher

class BeJSONTestCase: XCTestCase {
    func testBeJSON() {
        //expect("{}").to(beJSON())
        //expect("{").toNot(beJSON())
        //expect(10).toNot(beJSON())
    }
}
